{% extends 'layout.html.twig' %}

{% block title %}Consulter les annonces{% endblock %}

{% block body %}
    <div class="homeSection">
        <div class="row justify-content-center p-3 w-100 m-0">
            <div class="col col-md-4 col-lg-10 loginBlock h-100 p-2">
                <h2 class="loginTitle mb-3 text-center">Annonces</h2>
                    {% include 'offers/_partials/search.html.twig' %}
                <div class="row p-3 w-100 m-0">
                    {% for offer in offers %}
                        {% include 'offers/_partials/offer.html.twig' %}
                    {% else %}
                        <div class="col-12">
                            <p class="alert alert-primary">
                                Aucune annonce disponible. Déposez la votre <a href="{{ path('offers_new') }}">ici</a>.
                            </p>
                        </div>
                    {% endfor %}
                    <div class="col-12 mt-2 d-flex justify-content-center">
                        <div>
                            {{ knp_pagination_render(offers) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
